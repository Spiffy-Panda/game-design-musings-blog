# PLAN — index

Index of plans. One line per slug; the detail lives in `plans/PLAN-<slug>.md`.

## Active

- [PLAN-blog-site](plans/PLAN-blog-site.md) — the published directory site: landing page, preview server, Pages deploy, and the musing build framework (`MUSING-CONFIG.json` + per-folder `build-musing.py` + generator), now with multi-page musings (sub-pages) and a React/Tailwind sub-site for the MSL approaches (`approaches-app/`).
- [PLAN-msl-explorations](plans/PLAN-msl-explorations.md) — overnight gallery of interactive HTML "explorables" for MSL technical aspects (solvency sim, jumpgate topology, enemy schedule, utility-AI fit, …); staged in `explorations/`, **not** wired into the site build, awaiting review + promotion.
- [PLAN-logical-magic](plans/PLAN-logical-magic.md) — **LoMa**, a magic system built on first/second-order logic (advanced tiers: monads, game semantics, the limit theorems); MDYN-grade grounded calculations without field equations. Lives in `logical-magic/` (HTML-first: pitch + worksheet + assize + the vignette companion page `vignettes.html`, all shipped + registered alongside `thaumodynamics/`). Next: the Grimoire interactive toy (the one remaining candidate).
- [PLAN-space-feudal](plans/PLAN-space-feudal.md) — **Space Feudal (SF)**, feudalism as an equilibrium: invented physics (lane-graph FTL, fading fuel, no ansible) recreate the six economic problems the fief solved, and lords condense out of the sim above an X-style cockpit. Lives in `space-feudal/` (HTML-first: brief + 27-row old↔new ledger + siege-doctrine page w/ the Harrow system map + the Loom consequences page, all shipped + registered). Next: candidate explorable/event-chain pages.
- [PLAN-midi-drum](plans/PLAN-midi-drum.md) — **MIDI Drum Coach (MDC)**, a practice tool as a game-design musing: the browser connects to an e-kit (Web MIDI) and a *legible* director suggests the next groove — step-grid notation, millisecond judging, rules it explains out loud. Lives in `midi-drum/` (HTML-first: hub w/ claims `MDC.1`–`MDC.5` + the playable Coach, shipped + registered; works kit-free via keyboard/click pads). Next: the Fill Director, accent judging, MIDI-learn mapping.
- [PLAN-adventuring-guild-teller](plans/PLAN-adventuring-guild-teller.md) — **Adventuring Guild Teller (AGT)**: you are the guild's front desk — a papers-please checking loop built for flow, an after-hours guild floor where all the decisions live, and an authorable townee-fishbowl you read about in nightly summaries. Lives in `adventuring-guild-teller/` (HTML-first: hub + pitch read-back w/ claims `AGT.1`–`AGT.12` + desk research w/ risks `AGR.1`–`AGR.6`, shipped + registered). Round 1 of corrections folded in (nine claims settled — floor never ticks; no death, gear-loss seeds quests; bios hold the detail). Next: settle `AGT.1`/`.5`/`.7` (round 2), then the Morning Queue paper prototype.
- [PLAN-morning-queue-tiers](plans/PLAN-morning-queue-tiers.md) — **Morning Queue tiers (MQT)**: refactor the Godot desk prototype along code/script/data lines — a .NET/C# `core/` (typed model, validator, shift generator, `dotnet test`), GDScript reduced to node scripting, authored strings out of scripts into `data/`. Proposed 2026-07-15; gated on rulings `MQT.D1` (Web-invariant rewrite vs out-of-engine baker), `MQT.D2` (RNG rebaseline), `MQT.D3` (theme .tres).
- [PLAN-godot-test-harness](plans/PLAN-godot-test-harness.md) — **Godot test harness (GTH)**: a reusable input/inspect/capture **MCP** (+ prescripted scenario runner) for any .NET/GDScript-mixed Godot project. Synthetic clicks by location or `test_id`, hit-stack + consumption-order reporting, clickability/hit-box/offscreen reports, and token-frugal capture (reference-by-path, settle-then-shoot, `if_changed` dedup). Two thin drivers over one pure-GDScript in-engine core (addon + gated WS bridge). **In-engine core + prescripted runner built & verified in the fishbowl 2026-07-15** (`GTH.M0`–`M4`: synthetic click → sim tick, location-click → roster select, hit/consumption reports, sha-dedup + annotate capture); the external **MCP server** (`utils/dotnet/gth-mcp-server/`, .NET 8, dependency-free) is built, registered in the repo `.mcp.json` as `gth-fishbowl` (Rule-7-clean, relative + self-defaulting), and **driven live by a model for ~213 calls on 2026-07-16** — all seven gates ruled, all requirements met, nothing blocked on a decision. **The `GTH.B*` bugs are all six fixed & verified 2026-07-16** (the four the harness's own first use in anger turned up, plus a window resize-lock and a minimize guard — minimize was measured to freeze the framebuffer while the sim runs on, so dedup called a changed screen "unchanged"); covered by a green 34-step regression scenario + an extended `--selftest`. The **Mode-B observed trace is built** (`trace: true` — it watches every Control's `gui_input` and scores itself against Mode A's prediction), discharging `GTH.D3`. **`GTH.Q4` ruled full convergence and executed 2026-07-16:** the addon is now shared tooling (canonical `utils/godot/gd_test_harness/`, fanned out by `utils/python/sync_gth_addon.py --check`), `morning-queue/` has adopted it and **retired its bespoke `DevHarness.gd`**, and both projects are registered in `.mcp.json`. That ruling paid for itself within the hour — the second consumer turned up **`GTH.B7`/`B8`** (a refused click and a `wait_for` timeout both reported as *passes*) on its first run, in a release-old codebase one consumer had never tripped. `GTH.Q1` answered; `GTH.Q2`/`Q3` + the C# facade **closed as YAGNI**. **Open: nothing** — all rulings are in. Addon is additive to the fishbowl's F9 DevHarness; server is shared `utils/` tooling (convergence = `GTH.Q4`).
- [PLAN-fishbowl-postings-outings](plans/PLAN-fishbowl-postings-outings.md) — **Postings & outings (PNO)**: the half of the fish-bowl the first pass deferred — a **board** of standing postings (villager needs become paper via the storylet system) and **outings** (an adventurer takes a posting, swaps day-plan into an off-screen site of *legs, not floors*, then cools down and re-enters daily life). Resolves `VFB`'s away-flag stand-in and its one-way trapdoor. Keyed on `AGT.12`: "die trying" = **rout, not death** — gear lost, and the lost gear files its own retrieval posting. Spec written 2026-07-16; **all nine rulings `PNO.D1`–`PNO.D9` landed 2026-07-16 and `PNO.M1` (the board) landed the same day** — a posting files on day 2 and expires on day 6 in the live town, every transition carrying a because-list (expiry synthesizes its own, being a board mechanism rather than a rule). Building it turned up that the `post` effect had been a **silent no-op shipping in a green build** — `StoryletEffectDto` had no `Post` field, so the key was dropped and `Board.File()` had zero callers, while the chronicle still printed "filed a posting"; the effect union is now enforced at load. **`PNO.M2` (outings) is next and carries two hard preconditions:** a Panda ruling + `DEV-LOG.md` entry **before** the three pinned hash literals move, and an **unresolved collision** between the ruling that restlessness ships broken (M2 is its discharge) and the spec's own finding that a `haunt`-tagged site visit burns restlessness off *at the site* — "precisely backwards for an outing". `PNO.D1`/`D3`–`D9` adopted on the recommendation (strike "quest" as a type name; self-served board; silence while away; rout seeds a retrieval posting; solo outings); **`PNO.D2` ruled *against* the recommendation** — `data/` becomes the live all-features town and the golden day moves out to a frozen secondary fixture (`tests/towns/golden-town/`), which keeps all 22 tests green verbatim *and* fixes a golden master that had been tracking the data it was meant to pin. Re-drift-checked before building: 21/22 claims confirmed, and **three of the spec's own supporting arguments were falsified and struck in place** (the phase machine's "works unchanged", site-as-place's "zero engine changes", `chronicle_since`'s fitness for the retelling). Ships with a `.doc-reader.md` (the kickoff prompt to paste into a fresh chat) + a `.handoff.md` (the implementing agent's operating brief: the gate, the hard rules, a sub-agent delegation doctrine, and a ready-to-fire reader that drift-checks the spec's 22 code claims). Isolation from `morning-queue/` inherited and reaffirmed (`PNO.D9`).
- [PLAN-village-fishbowl](plans/PLAN-village-fishbowl.md) — **Village fish-bowl (VFB)**: AGT's Roster-pillar town sim as a playable observatory — Godot 4.6 .NET prototype (C# engine-free core / GDScript UI / JSON data) running clockwork day-plans + pressure meters + storylet events into a chronicle and dawn summary; data readouts + debug knobs only, plus the creation menus' first pass; no desk, no floor. Machinery chosen via studies `FBS.1`–`FBS.6`; proposal read-back `FB.1`–`FB.10` on the musing. **First release built 2026-07-15** — rulings `VFB.D1`–`VFB.D4` + `FB.8` adopted; M0–M3 gate-checked, M4 in place; code + frozen interfaces in `adventuring-guild-teller/fishbowl/` (see `FISHBOWL.md`). **The live town was rebuilt wholesale 2026-07-16** (Panda's ruling) — now 18 townees / 16 places (8 board) / 4 adventurers / 50 storylets / 49 regard edges, with the old town preserved twice: at `b086c96`, and frozen in-repo as the fixture `tests/towns/golden-town/` that pins the new one. **`VFB.Q1` is CLOSED, not tuned — it is saturated**: specified as "avg distinct tellable lines/night" but computed *inside* a summary already cut to `summary_lines`, so it is `min(pool, 5)` averaged over a pool of ≥20. It reads a flat 5.00 and **still reads 5.00 with 30 of 46 rules deleted**. The old ~4.4 figure is superseded, not falsified, and the comparison is itself the finding. Replaced by the deliberately **un-scored** `Api/Variety.cs` (distinct rendered *texts*, told/fired, never-told, repeat vs N−1 and vs any prior night) — no threshold, because a threshold invites tuning toward it. Workstream is hard-isolated from `morning-queue/`.

## Completed

_(none yet)_

---

### How to use this index

- One line per plan slug; detail lives in `plans/PLAN-<slug>.md`.
- Move a slug from **Active** to **Completed** when its plan is fully ticked off.
- Add a new slug here the moment its `PLAN-<slug>.md` adds or completes a top-level item.
